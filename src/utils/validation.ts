// Re-export validation schemas and types from the types directory
export {
  passwordSchema,
  registerSchema,
  loginSchema,
  updateUserSchema,
  RegisterInput,
  LoginInput,
  UpdateUserInput
} from '../types/auth.types';